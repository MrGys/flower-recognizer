Prerequisits:
Python 3.x
Pip
Pipenv (pip install pipenv)

Install Dependencies

- pipenv install

Build new virtual environment

- pipenv shell

# Run the application

```bash
export FLASK_APP=index.py
```

```bash
flask run
```
